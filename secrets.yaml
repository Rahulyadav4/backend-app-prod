apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
stringData:
  # FIX SDE-2: All secrets here — not in source code
  # Apply with: kubectl apply -f secrets.yaml
  JWT_SECRET: "local-minikube-secret-key-32chars!!"
  APP_USERNAME: "admin"
  APP_PASSWORD: "admin123"
